function Soma(){
   result.value = parseInt(v1.value) + parseInt(v2.value);
}

function Diminuir(){
    result.value = parseInt(v1.value) - parseInt(v2.value);
}

function Multiplicar(){
    result.value = parseInt(v1.value) * parseInt(v2.value);
}

function Divisao(){
    result.value = parseInt(v1.value) / parseInt(v2.value);
}